import React from "react";

interface ListingCardProps {}

const ListingCard: React.FC<ListingCardProps> = ({}) => {
  return <div className="bg-bg-light-dimmed aspect-square p-1">hej</div>;
};

export default ListingCard;
